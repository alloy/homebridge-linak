{
  "private": true,
  "displayName": "Linak Desk Control",
  "name": "homebridge-linak",
  "version": "1.0.0",
  "description": "A short description about what your plugin does.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/alloy/homebridge-linak"
  },
  "bugs": {
    "url": "https://github.com/alloy/homebridge-linak/issues"
  },
  "engines": {
    "node": ">=12.13.0",
    "homebridge": ">=1.3.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint src/**.ts --max-warnings=0",
    "watch": "npm run build && npm link && nodemon",
    "build": "tsc",
    "build:reload": "runuser -u pi -- npm run build && hb-service restart",
    "build:clean": "rimraf ./dist && npm run build",
    "prepublishOnly": "npm run lint && npm run build:clean"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "dependencies": {
    "deskbluez": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "@typescript-eslint/eslint-plugin": "^4.16.1",
    "@typescript-eslint/parser": "^4.16.1",
    "eslint": "^7.21.0",
    "homebridge": "^1.3.1",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  }
}
